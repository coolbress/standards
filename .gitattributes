# 개행은 저장소 안에서 **LF 로 정규화**한다. 커밋된 이 파일만으로 돈다 — 로컬 설정이 필요 없다.
# ⚠️ 지금 CRLF 는 **0건**이라(`git ls-files --eol`) 이 줄이 기존 파일을 다시 쓰지 않는다.
* text=auto eol=lf

# ── 생성물 — 리뷰 화면에서 접는다 ──────────────────────────────
# `linguist-generated` 가 붙으면 GitHub 이 **diff 에서 접는다**
# (linguist 1차 문서: *"these files are suppressed in diffs"*).
#
# 🔴 **왜 이 저장소에 특히 값을 하나**: `direction/05` 는 `ci / diff-size` 에서
# **이 저장소를 면제**했고 그 이유가 *"URL 원장 재감사 한 번이 964줄이다"* 였다.
# 그 964줄이 아래 첫 항목이다. 면제는 **줄 수를 세지 않는 것**이고, 이건 **눈에 안 띄게 하는 것**이다 —
# 다른 층위라 겹치지 않는다.
#
# 🔴 **쓰는 도구가 있는 것만 적는다.** 손으로 관리하는 파일을 접으면 **진짜 수정이 숨는다.**
# (`corpus/_meta/sources.jsonl`·`audit/retrieval-cases.jsonl`·`audit/model-ab-raw/MANIFEST.tsv`·
#  `audit/before-manifest.tsv` 는 쓰는 도구가 없어 **일부러 뺐다.**)
ROUTES.jsonl                                    linguist-generated
audit/external-url-status.jsonl                 linguist-generated
audit/after-manifest.tsv                        linguist-generated
corpus/census-data/provenance/snapshot-manifest.json linguist-generated
