# 다음 세션 인계 — 만들기 시작

## 🔄 2026-09-01 (7차) — **`R5-46`·`R5-45` 에 계기를 붙였다** (둘 다 **부분**)

### ⚖️ `R5-46` — 심판을 표시하고 **조합**을 막는다

**심판 = `.github/workflows/*.yml`·`*.yaml` · `ruleset.json`**(벽과 그 배선 · 재사용 워크플로 **핀** 포함).
🔬 **`.yaml` 도 지킨다** — Actions 가 둘 다 읽으므로 하나만 지키면 **확장자만 바꿔 빠져나간다.**
`tools/check_referee_isolation.py` 가 **심판과 선수가 한 PR 에 같이 들어오면** 실패시킨다.
**심판만 바꾸는 PR 은 통과한다** — 밀반입할 게 **없기** 때문이다.

⚠️ **좁게 골랐다 — 규율이지 누락이 아니다**: `AGENTS.md` 는 §Code Review Rules 를 **이미**
`workflows/pr-review.yml` 이 핀으로 지키므로 **두 번 안 막는다** · `tools/test_*.py` 는 도구와
**한 단위**라 갈라 막으면 **TDD 가 불가능해진다**.

> 🔬 **규칙이 자기 자신에게 먼저 물었다** — `ci.yml` 배선이 **심판 변경**이라 도구와 같은 PR 에
> 못 넣었고 **PR 을 둘로 갈랐다.**

### 💸 `R5-45` — 유지비를 잰다. **첫 눈금 93%**

`tools/check_harness_cost.py` — 하네스 **318** · 제품 **23** → **93%** · 제품 저장소 **1개**.

🔴 **이 수를 나쁜 결과로 읽지 마라.** *하네스가 비싼 것* 과 *제품이 아직 하나뿐인 것* 은
**같은 숫자로 보인다** — 그래서 제품 저장소 수를 같이 찍는다.
**임계값은 안 긋는다**(`R5-2` 규율) — **2~3개 프로젝트가 쌓인 뒤에** 지점을 정한다.

> 🔬 **상한을 100 으로 뒀더니 `standards`(208건)에서 실제로 닿아 FAIL 이 났다** — 계기가 제 일을 했다.
> *조용히 잘린 목록으로 비율을 내면 거짓말이 된다.*

## 🔴 2026-09-01 (6차) — **처분 기록 없이 머지된 지적이 18건이었다**

*"오늘 물린 것 다 고쳤나"* 를 물어서 원자료를 다시 셌더니 **아니었다.** 그래서 *"이게 반복 안 되게 할 장치"* 를
재봤고, **기계로 잡히는 형태**가 하나 있었다 — **머지된 PR 의 *마지막 커밋* 에 달린 지적**은 그 뒤 커밋이
없으므로 **구조적으로 안 고쳐진 것**이다. 판단이 안 들어가서 **오탐이 0** 이다.

| | |
|---|---|
| 실측 | **PR 12개 · 지적 18건**(P1 포함) |
| 🔬 | **`#211`(벽을 세운 PR)** 과 **`workflows#66`(리뷰어 지시 핀)** 도 **P1 을 처분 기록 없이** 머지했다 |
| 계기 | `check_review_signal` 의 `merged_open` · `merged_open_prs` |

⚠️ **"무시했다" 가 아니라 "적을 자리가 없다" 이다.** 지적이 틀렸거나 범위 밖일 수 있는데 **그걸 적을 곳이 없다.**
그 자리를 만드는 것이 **`R5-40` ⓒ 리뷰 처분 계약**이고, 이 수가 **그 층이 기다리던 재료**다.
🚫 **벽으로는 안 만들었다** — *findings 로 안 막는다* 는 `IPW-020`(13종 중 12종이 signal ratio 60% 미만)으로
사전등록된 결정이다. **계기판이지 판정선이 아니다.**

> 🔴 **같은 뿌리에 오늘만 네 번 물렸다.** ① 벽의 판정 ② 이 계기의 **지도 만들기**
> ③ 이 계기의 **조회 키**(지도는 `original` 로 만들고 조회는 `commit_id` 로 했다 — 전부 빗나가
> 대리지표가 **늘 0** 이었다) ④ 그리고 **③ 은 ② 를 고친 바로 그 커밋에서 놓쳤다.**
> 실측: 조회 키까지 맞추니 `touched_after` 가 **44% → 58%** 로 올랐다.
>
> 🔬 **그래서 이번엔 *전수로* 찾았다** — 두 저장소에서 `commit_id` 를 읽는 자리를 전부 grep 했고,
> 남은 하나(`pr-review.yml` 의 **리뷰 객체**)는 **그 필드가 맞다**(리뷰 객체엔 `original` 이 없고 안 움직인다).
> **한 곳을 고칠 때 같은 값을 읽는 다른 줄을 안 보는 것** — 이게 오늘의 세 번째 반복 형태다.

## 🔴 2026-09-01 (4차) — **벽이 보증한다던 것이 거짓이었다**

`third-party / review` 의 계약은 *"제3자가 **이 커밋** 을 봤다"* 하나인데 **안 보고도 초록이 됐다.**

**기제** — GitHub 은 PR 에 새 커밋이 붙으면 **살아 있는 리뷰 댓글의 `commit_id` 를 현재 head 로 옮긴다.**
판정이 그 필드를 봤다 → **옛 리뷰의 인라인 댓글 하나로 그 뒤 모든 푸시가 자동 초록.**

| | |
|---|---|
| 실측(두 번 재현) | 새 커밋 push 후 **20초에 SUCCESS** · 진짜 리뷰는 **140~160초 뒤** |
| 고친 뒤 | **160~220초** — 즉시 초록이 사라졌다 |
| 고친 곳 | [`workflows#71`](https://github.com/coolbress/workflows/pull/71) — `original_commit_id`(안 움직인다)를 본다 |
| 🔴 소비자 핀 | [`#225`](https://github.com/coolbress/standards/pull/225) — **핀을 안 올리면 여기선 안 고쳐진다** |

🔬 **왜 안 걸렸나 — 시험이 그 경로를 한 번도 안 태웠다.** `tests/pr-review-attest.sh` 는 리뷰 객체·이슈
댓글 쪽 음성 시험이 촘촘한데 **리뷰 댓글은 `rc.json` 이 늘 `[]`** 였다.
🔴 **앞의 다섯과 다른 점** — *1·2 는 **검사가 아예 없는 거짓 초록**(목록을 보면 보였다) · 3 은 **성급한 부재 판정** · 4·5 만 **거짓 빨강**이다.* 여섯째는 **검사가 있고 초록을 말하는데 그 초록이 거짓**이라 **보이는 곳이 없었다.** (분류는 [`direction/07`](direction/07-design-rules.md) 이 정본)
전문은 [`direction/07`](direction/07-design-rules.md) §반복해서 무는 실패.

## 🔬 2026-09-01 (5차) — **리뷰가 11회차까지 갔고 findings 32건이 전부 진짜였다**

`R5-37` 계기([`#224`](https://github.com/coolbress/standards/pull/224)). **findings 32건**(P1 3 · P2 28 · CodeQL 1).
큰 갈래 셋이 각 4건 — **지표가 한쪽만 셈** · **구조화된 줄을 부분문자열로 훑음** · **예시가 데이터로 세어짐**.

> 🔴 **처음엔 *"20건이고 다 해결했다"* 고 적었다. 둘 다 틀렸다.** 원자료를 세니 32건이었고
> **다섯은 손도 안 댄 채 머지했다**(머지 뒤 도착 둘 · 내가 놓친 셋 — [`#226`](https://github.com/coolbress/standards/pull/226) 에서 **같이** 고쳤다).
> **요약이 원자료보다 오래 살았다** — 이 저장소의 대표 결함을 내가 그대로 했다.

🔴 **둘째를 네 번 물리고서야 고치는 자리가 틀렸다는 걸 알았다** — 칸을 하나씩 때우면 다음 필드에서 또 난다.
`parse_marker` 로 **한 번 쪼개니** 끝났다. **필수 칸을 산문이 채울 수 있으면 그건 필수 칸이 아니다.**
🔴 **셋째에서 방향이 뒤집혔다** — 예시를 막는 *순서* 를 잘못 놓자 **진짜 회부가 사라졌다.** 오탐보다 나쁘다.

> 🔴 **내 시험이 두 번 가짜였다.** 소스 문자열만 봐서 `if False:` 로 죽여도 초록이었고,
> 순수 함수만 직접 봐서 호출부 변이를 못 잡았다. 둘 다 **행동 시험**으로 바꿨다.
> **그래서 고칠 때마다 변이를 넣는다** — 되돌리면 몇 건이 빨개지는지. 안 물리면 시험이 아니라 장식이다.

📊 **리뷰 신호 눈금**(`check_review_signal`): 리뷰된 PR **8 → 14** · finding **17 → 55**(P1 12 · P2 43).
`ENOUGH=20` 이라 **아직 판정선을 안 긋는다** — `R5-40` ⓒ 다음 층은 여전히 대기다.

## 🔬 2026-09-01 (2차) — **GitLab 프로브를 돌렸다** (`R5-44` 닫힘)

**비공개 + 무료 경로는 GitLab Free 에서 — 한 층이 빠진 채 — 선다.** 2026-08-30 에 **1차 문서로** 적어둔 것 중
넷이 맞았고 **하나가 틀렸다.** 전문은 [`GAPS` R5-44](audit/GAPS.ko.md).
🔴 **바닥 예외 하나를 명시했다**(제3자 리뷰 P2 가 잡았다): 묶음 12 는 시크릿을 **두 층**으로 요구하는데
(**푸시 보호=벽** · **gitleaks=그물**) **GitLab Free 는 그물만 준다** — 서버측 push rules 가 Premium 이라
**벽의 대체물이 Free 에 없다.** 시크릿이 **원격 이력에는 일단 닿는다**(머지는 막힌다).
*"실제로 선다"* 라고만 적으면 **바닥을 다 채우는 것처럼 읽힌다** — 그게 지적이었다.
🔬 **측정 조건을 먼저 쟀다** — `plan: free` · `trial: false`. **체험판이 걸려 있으면 Ultimate 기능이 켜져
측정이 통째로 무효다**(오른쪽 패널의 *"Start a Free Trial"* 을 누르면 그렇게 된다).

| | 잰 것 | 결과 |
|---|---|---|
| ⓐ | `push_access_level: 0` 에 push | ✅ **Owner 도 Developer 도 거부** — 대조군(비보호 브랜치)은 통과 |
| ⓐ | `allow_force_push: false` | ✅ 보통 push 는 통과, **force 만** 거부 |
| ⓑ | 빨간 파이프라인 → 머지 | ✅ `detailed_merge_status: ci_must_pass` · **405** |
| ⓑ | **파이프라인이 아예 없음** → 머지 | ✅ **405 — fail-closed** |
| ⓑ | 외부 커밋 상태 `success` → 머지 | ✅ `mergeable` · **200** — **러너 밖 CI 로도 벽이 선다** |
| ⓒ | Free SAST · 시크릿 탐지 **잡** | ✅ 돌고 **5건 찾는다** — 🔴 그런데 **`allow_failure: true`** 라 초록으로 끝난다 |
| ⓒ | 우리가 붙인 게이트 | ✅ 발견 5건 → 빨강 → 머지 **405** · 이력이 깨끗하면 초록 → **200 머지됨** |
| ⓓ | 분 | ✅ 3잡 **62초** — 400분/월 대비 PR 당 **약 1.0분** |
| 🔴 | 시크릿 **푸시 보호** | 🔴 **안 돈다** — 설정은 `true` 로 받는데 시크릿 5종이 **전부 통과** |

⚠️ **`merge_status` 를 읽으면 안 된다** — 빨간 상태에서도 `"can_be_merged"` 다(충돌 여부만 본다).
**`detailed_merge_status` 가 정본.**

### 🔴 새로 드러난 관문 둘 — *"토큰만 있으면 돈다"* 가 아니었다

- **신원 확인(카드·전화) 전에는 잡이 하나도 안 돈다.** 파이프라인이 **잡 0개로 즉시 실패**하고
  **REST 는 이유를 안 준다**(`yaml_errors: null`) — GraphQL 의 `failureReason` 만 말해준다:
  *"The pipeline failed due to the user not being verified."*
  🔬 **이 빨강을 우리 코드의 실패로 읽으면 못 잰 것을 실패로 읽는 것이다** — 이 저장소의 대표 실패형태다
- **그룹 소유 프로젝트는 프로젝트 액세스 토큰을 못 만든다**(Owner 인데도 400 · 그룹 토글을 켜도 400).
  **개인 네임스페이스는 된다**(Developer 30 → 201) → ① 의 미검증 전제가 이렇게 풀렸다. **계정 둘은 필요 없었다**

> 🔴 **오늘도 같은 병이 하나 잡혔다** — `direction/05` 가 *"시크릿 탐지 + **푸시 보호** Free"* 라고 적고 있었다.
> **1차 문서를 읽고 쓴 요약이 *도는 것* 보다 오래 살았다.** 고쳤다.
> **문서에 있는 것과 도는 것은 다른 문장이다** — 이번엔 *설정 API 가 200 을 주는 것* 까지 그랬다.

## 🔄 2026-09-01 (3차) — **`R5-37` 을 ⓑ 로 닫았다**: 회부 수단을 *하나 더* 열었다

결정당 이슈로 바꾼 뒤 **회부 4건**이 쌓였다(전부 닫힘 · 전부 커밋된 기록에 인용됨). **계기는 움직였다.**
그런데 같은 기간 PR 은 스무 건이 넘고 **그 안에서 오간 판단은 이슈로 안 올라온다** — 이슈를 여는 값이
*"PR 안에서 이미 묻고 답한 것"* 에는 과했다. 그래서 **PR 본문에 표시 한 줄**을 수단으로 추가했다:

```
회부: decision:input — 어휘를 A 로 갈까 B 로 갈까 → 답: A (채널: 대화)
```

🚫 **이슈 수단은 안 걷어냈다** — PR 이 없는 회부(착수 전 · 권한 없음)는 여전히 이슈다.
🔴 **행동하는 자리에 적었다** — [`AGENTS.md`](AGENTS.md) §ASK FIRST 가 두 수단과 형식을 적는다.
*수단은 적어뒀는데 아무것도 발화시키지 않는* 상태가 이 격차를 연 원인이었다.

> 🔴 **이번엔 네 축이 다 강해지지 않는다 — 그것도 적었다.**
> 분모 순증 · ⓑ 동일 · ⓒ **동률**(🔴 정정 2026-09-01 — 처음엔 *"오히려 낫다"* 고 적었는데
> **PR 본문도 저장소 밖이라 머지 뒤에 고쳐진다.** 이슈와 같은 약점이다) · **작업량은 줄어든다.**
> 2026-08-29 의 변경(넷 다 강화)과 **다르다.** *"이번에도 강화다"* 로 적으면 §판정 기준의
> *"결과를 보고 기준을 옮기지 않는다"* 를 **말로만** 지키는 것이 된다.
> 🎯 **완화인지 아닌지는 눈금이 답한다** — `referrals_total` 이 안 늘면 완화였다.
> **첫 눈금 4**(이슈 4 + PR 표시 0). 판정은 `direction/04` 의 **2주 관측** 때.

🔴 **다만 원천이 가변이다** — 검사는 *지금 본문에 남아 있는* 표시만 본다. 머지 뒤에 지우면
`referrals_total` 이 **조용히 줄고 다리도 못 잡는다**(제3자 리뷰 5회차가 물었다).
지속화 수단(커밋 메시지 · 별도 원장 · 그대로 두기)은 **눈금이 5건을 넘으면** 정한다 — `R5-47`.
지금 정하면 **표시 0건 상태에서 상상으로 고르는 것**이고 그건 이 저장소가 반복해 기각한 형태다.

## 🔒 2026-09-01 — **ⓒ 제3자 리뷰 벽이 섰다** (그리고 세우는 데 다섯 번 틀렸다)

`third-party / review` 가 **required check** 다. 두 저장소 모두 `enforcement=active` · `bypass_actors=0`.

> **이제 코덱스가 이 커밋을 보지 않으면 머지가 안 된다.** 로컬 `/review` 와의 차이가 여기서 끝났다.

### 무엇이 벽이고 무엇이 아닌가

| | |
|---|---|
| ✅ 보증한다 | **코덱스가 *이 커밋* 을 봤다** · 리뷰어 지시가 이 PR 안에서 무르게 안 바뀌었다 |
| 🚫 보증 안 한다 | **잘 짰다**(findings 로 안 막는다) · ***읽혔다*** |

**findings 로 안 막는 이유는 실측이다** — `IPW-020`(MSR '26 · PR 3,109 · 에이전트 13종):
**13종 중 12종이 signal ratio 60% 미만**, 닫힌 PR 의 **60.2%가 0~30% 소음대**.
`IPW-019`: AI 생성 PR **33,596건 중 61.38%가 리뷰 기록조차 없다** ← **이 벽이 막는 구멍이 그것**이다.

### 신호 셋 — 하나만 보면 놓친다

| | 신호 | 실측 |
|---|---|---|
| ① | 리뷰 객체 | 지적이 **있을 때** · 5~9초 |
| ② | 표식 `completed` | 🔴 끝나고도 `running` 에 멈춘다 · 한도 걸리면 **안 실린다** → 예비용 |
| ③ | 완료 댓글 | 🔴 **지적 0건이면 유일한 신호** · `**Reviewed commit:**` 으로 **커밋에 묶는다** |

### 🎯 제3자가 첫 실전에서 문 것 — **셋 다 진짜였다**

1. **P1** — 리뷰어의 지시(`AGENTS.md` §Code Review Rules)를 **리뷰당하는 PR 이 고칠 수 있었다.**
   🔴 *이 저장소가 스스로 적어둔 원칙*으로 이 저장소를 잡았다 → **지시 변경 + 다른 변경 조합을 막는다**
2. **P2** — `IPW-007` 의 **F1 → *놓친 비율* 오역**(`1 − F1` 은 위음성률이 아니다) → 원산지 + 파생 넷 정정
3. **P2** — **검증 안 된 2차 수치 인용**(CRA 처리율) → 뺐다

### 🔴 벽을 세우는 데 다섯 번 틀렸다 — 전부 *못 잰 것을 통과·실패로 읽음*

허용목록 → 시크릿 `required` → **3분 일찍 재고 "안 온다"** → **지적 0건이면 리뷰 객체가 안 생김** → **취소가 빨강으로 읽힘**.
기록은 [`direction/07`](direction/07-design-rules.md) §반복해서 무는 실패.

### 계기 하나가 늘었다

`tools/check_review_signal.py` — **네트워크(CI 밖)**. **첫 눈금**(계기를 붙인 순간): 리뷰된 PR **3** · finding **9** · P1 5 · P2 4. **세션 끝 눈금**: 리뷰된 PR **8** · finding **17** · P1 9 · P2 8 · **P0 0** · PR당 **2.1**.
🔴 **판정선은 안 긋는다** — `ENOUGH=20` 을 넘으면 계기가 스스로 논의를 열자고 말한다.

### ⚠️ 알아둘 것

- **코덱스 한도를 넘기면 리뷰가 안 붙어 머지가 막힌다.** 기다리거나 룰셋에서 그 검사만 뺀다(관리자·1분)
- **보안 검토는 Plus 에 아예 없다**(1차 `CODEX-SECURITY-REVIEW-AVAILABILITY`) · **크레딧으로도 안 산다**.
  ⚠️ 우리 계정이 Plus 인지 Pro 인지는 **안 쟀다**. **벽은 코드 검토만으로 선다**(실측)


> 작성 2026-08-24 · 갱신 **2026-08-30 (2차)** · **이 문서는 낡는다.** 만들 것이 하나씩 끝나면 갱신하고,
> 전부 끝나면 지운다. 정본은 [`direction/`](direction/)이고 이 문서는 **작업 지시서**다.

## ✅ 아스날이 처음으로 다 켜졌다 (2026-08-30 · 일곱 개 `enabled`)

**설치했더니 두 가지가 한꺼번에 드러났다.**

**① `-hooks` 는 한 번도 설치된 적이 없었다.** 설계표는 *"인식 · 🪝 SessionStart 훅 **둘** ·
🟢 결정적 · ✅"* 라고 적혀 있었는데 **운영자 훅 쪽은 이 기계에서 안 돌고 있었다.**

**② 깔았더니 `✘ failed to load` 였다 — 만든 이후 한 번도 뜬 적이 없다.**

> **"Duplicate hooks file detected … The standard `hooks/hooks.json` is loaded automatically,
> so `manifest.hooks` should only reference *additional* hook files."**

`hooks/hooks.json` 은 **자동 발견**된다. `plugin.json` 에 또 적어서 **두 번 로드**됐다.

🔴 **검증 도구 셋이 다 초록을 말했다** — `claude plugin validate` ✔ ·
`claude plugin details` 가 `Hooks (1) SessionStart` 로 정상 표시 · 우리 `plugin-manifests.sh` 통과.
**설치하고 재시작해야만 보인다.** 이제 `plugin-manifests.sh` 가 막는다(v0.6.0).

> **선언한 것과 도는 것은 다른 문장이다** — 오늘 **일곱 번** 만났고 이번 것이 가장 나빴다
> ([`direction/07`](direction/07-design-rules.md) §반복해서 무는 실패).
> **문서가 ✅ 라고 적는 것과 도는 것은 다르다.**

**지금**: `coolbress-standards` **0.6.0** · `-hooks` **0.6.0** · `ponytail` 4.9.0 ·
`taste-skill` 1.0.0 · `last30days` 3.21.1 · `mattpocock-skills` 1.2.3 — **전부 `✔ enabled`.**

### 🔴 여덟째 — *"심볼릭 링크 수작업이 사라졌다"* 가 **거짓이었다**

`/kickoff` 이 메뉴에 **두 개** 떴다:

| 항목 | 무엇을 읽나 |
|---|---|
| `/kickoff` (user) | 🔴 **작업 트리** — `~/.claude/commands/kickoff.md` → `~/workflows/commands/`(심볼릭) → `plugins/standards/commands/` |
| `/coolbress-standards:kickoff` | 설치본 캐시 |

**마켓플레이스 이전의 수작업 심볼릭이 그대로 남아 있었다.** 그리고 이미 **서로 다른 답을 주고 있었다** —
작업 트리는 오늘 고친 *비공개 3경로* 를 담고, 설치본 0.6.0 은 옛 *"공개로 만드는 것뿐"* 을 담았다.

🔴 **사용자 레벨 커맨드는 정본이 될 수 없다** — **브랜치를 옮길 때마다 답이 바뀐다.**
설치본은 판이 박혀 있어 그렇지 않다. **배포하는 물건의 정본은 설치본이다.**

**고침**: `rm ~/.claude/commands/kickoff.md && rmdir ~/.claude/commands`.
⚠️ 되돌리기 쉽다 — 심볼릭 하나이고 원본은 저장소에 그대로다.

> **선언한 것과 도는 것은 다른 문장이다** — 이번엔 **인계 문서가 사라졌다고 적은 것이 안 사라져 있었다.**
> 여덟 번째이고, 앞의 일곱과 달리 **거짓을 문서가 직접 적고 있었다.**

## 🚀 2026-08-30 — 네 층을 전부 훑었다 (PR **43건** · 저장소 4곳)

**어제는 층을 세웠고, 오늘은 세운 것이 실제로 도는지 봤다.** 여덟 번 물었고 여덟 번 다 뭔가 나왔다.

| 층 | 훑은 범위 | 나온 것 |
|---|---|---|
| 🔒 **벽** | 룰셋 규칙 종류 전부 · 서버 설정 전부 · 워크플로 본문 | **zizmor** 벽으로 · `code_scanning`·`linear_history`·**태그 룰셋** 등재 · `artipacked` 13곳 |
| 📦 **상자** | 4아키타입 **실제 렌더** · 파일 내용 전부 | 아키타입 **6→4**(만들 수 없는 것 제거) · `Dockerfile`+진입점+로깅 · `.gitattributes` · 개인설정 ignore · 파이썬 범위 |
| 🤝 **안내** | 매니페스트 · frontmatter · 스킬 | `floor-check`·`review` 를 **`disallowed-tools` 로 읽기전용** · **무기고 안내** |
| 📚 **근거** | 스키마 · claim 구조 · 태그 어휘 · 검색 계약 · 센서스 방법론 | `[lit]` **타입화**(62건) · 센서스 불확실성 측정 |

**현재 판**: `project-template` **v2.12.1** · `workflows` **v3.9.0** · 플러그인 **v0.5.0** ·
`divcal` **v2.12.1** · 검사 **15종** · 파이썬 시험 **233** · 셸 시험 **15** · 열린 격차 **7**

### 🔴 오늘의 진짜 소득 — 같은 병을 **여섯 번** 만났다

| 있었나 | 안 돌았나 |
|---|---|
| `tests/security-setting.sh` | CI 배선 없음 |
| `extra-python-versions` 입력 | 아무도 안 넘김 |
| jinja 조건 | 그 파일은 `.jinja` 가 아니었다 |
| `PATCH` 200 | 서버가 조용히 무시 |
| `[lit]` 검사 | 범위가 실패 형태보다 좁음 |
| **`-hooks` 플러그인** | **설치된 적이 없음** ← 아직 살아 있다 |

**매번 CI 는 초록이었다.** 다섯은 검사가 됐고 규칙으로 적었다
([`07`](direction/07-design-rules.md)). ⚠️ **원인은 못 잡는다** — 셋은 편집 스크립트가 죽었는데
`&&` 뒤가 계속 돌아 생겼고, 그건 에이전트의 셸 습관이지 저장소의 성질이 아니다.

### 🎯 아스날 — **다음 순서가 정해져 있다** (`ARSENAL` §2c · `GAPS` R5-24)

2026 후보 셋(**Superpowers** 752k · **Frontend Design** 공식 277k · **Context7** 349k)을
훑고 **셋 다 기각**했다. Superpowers 는 겹치는 게 아니라 **TDD 강제**와 **subagent dispatch** 로
우리 결정과 **정면으로 부딪힌다**.

🔴 **그리고 지금 더 넣는 것 자체가 틀린 순서다.** 실측 `ours=1 / 15`.

> **① `-hooks` 를 켠다 → ② 몇 세션 돌린다 → ③ `python3 tools/check_skill_firing.py` 로 다시 잰다
> → ④ `ours` 가 안 움직이면 **그때** 무기를 본다.**
>
> ⚠️ ③ 은 **로컬 전사**를 읽는다 — 새 훅이 깔린 **뒤의 세션**부터 찍힌다.
> 켜자마자 재면 같은 수가 나오고, 그건 정보가 아니다.
> ⚠️ 안 움직이는데 무기를 더 넣으면 **상시 토큰만 늘고 발화는 그대로**다.

**기준선**: `skill_events=15 · distinct=12 · ours=1`(2026-08-30 · `kickoff` 한 번).

## 🚀 2026-08-29 (2차) — **배포 층이 섰다** (PR 46건 · 저장소 4곳)

**아침엔 정비였고, 오후엔 🤝 안내 층을 실물로 만들었다.** 네 층 중 마지막으로 비어 있던 칸이다.

```bash
claude plugin marketplace add coolbress/workflows
claude plugin install coolbress-standards@coolbress    # 실측: 상시 ~381 토큰
```

| 무엇 | 어디 |
|---|---|
| **플러그인 둘** — `coolbress-standards`(기본) · `-hooks`(옵트인) | `workflows` 가 **마켓플레이스**가 됐다. ~~심볼릭 링크 수작업이 사라졌다~~ 🔴 **거짓이었다 (2026-08-30 정정)** — 아래 §심볼릭 잔재 |
| **세션 시작 훅 둘** (만들 것 ⑦ ✅) | 🔴 **⑦ 은 하나가 아니라 둘이었다** — **프로젝트 관례**는 템플릿의 `.claude/settings.json`(v2.4.1 · 커밋되고 리뷰된다), **운영자 관례**는 플러그인 훅. 겹치면 두 번 찍는다 |
| **아스날 넷** | `mattpocock`(공식 마켓 SHA 핀) · `taste-skill`·`last30days`·`ponytail`(**우리 마켓이 SHA 로 직접 등재**) |
| `/review` (만들 것 ⑥ 🔄 재판정) | **만들지 않았다** — codex 플러그인이 이미 갖고 있다. **라우팅만** 정했다 |

**원칙이 셋 정밀해졌다** — 전부 1차 출처와 우리 실측이 충돌해 보인 자리에서 나왔다:

| | |
|---|---|
| **훅 vs 벽** | **훅은 *의도*(명령 문자열)를 보고, 벽은 *결과*(CI 가 실제로 돌린 것)를 본다.** 훅이 11건 중 0건이었던 건 **안 떠서가 아니라 뜨고도 못 봐서**다 |
| **되먹임 루프** | **안쪽 게이트는 진동하고 벽은 안 한다** — 게이트는 턴마다 돌고 고치는 것도 턴이라 고리가 닫힌다. 벽은 **푸시마다 한 번** |
| **강제 ✕** | 스킬을 `UserPromptSubmit` 로 주입하지 않는다 — **발화는 보장해도 준수는 아니다.** 인식=훅 · 준수=벽 · 그 사이=⑥ |

**재보고 접은 것 셋** — `caveman`(BSL 라이선스·상태 소유) · `path-scoped rules`(**새 파일 만들 때 안 뜬다** · [#82482](https://github.com/anthropics/claude-code/issues/82482)) · **복잡도 게이트**(CC≈LOC `R²=0.925`, `diff-size` 와 중복).

🔬 **오늘 두 번 같은 병에 걸렸다** — `ARSENAL` §2b 에 *"공식 마켓에 없으면 우리 항목을 직접 쓴다"* 를
**적어놓고 안 따라서** 첫 설치가 통째로 실패했고, 검사를 **끝 네 줄만 보고** 초록이라 판단해
**머지 전에 태그를 밀었다**([v2.4.0 이 잘못 섰다](https://github.com/coolbress/project-template/releases/tag/v2.4.0) — 지우지 않고 기록으로 남겼다).

### 🔴 그리고 **전제 하나가 틀린 채로 배포됐다** (2026-08-30 실측 정정)

***"커맨드는 사용자가 쳐야 한다"*** 를 깔고 설계했는데 아니었다. 1차 문서:

> ***"커스텀 커맨드는 스킬로 합쳐졌다"*** — `.claude/commands/x.md` 와 `.claude/skills/x/SKILL.md` 는
> **둘 다 `/x` 를 만들고 똑같이 동작한다.** **기본값은 사용자와 Claude 둘 다.**

**실물 확증** — 플러그인을 깔자 `kickoff` 이 **Skill 도구 목록에 떴다.**

👉 **`/new-project`(저장소를 만들고 실패하면 지운다)가 모델이 부를 수 있는 상태로 배포됐었다.**
문서의 `/deploy` 예시 그 자체다 — *"코드가 준비된 것 같다고 Claude 가 배포를 결정하는 걸 원치 않는다."*

**고쳤다** — `disable-model-invocation: true` 를 둘에 걸었다:

| | 모델 호출 | 왜 |
|---|---|---|
| `/kickoff` · `/floor-check` | ✅ 켜둠 | 묻거나 **읽기만** 한다. 끄면 **모델이 존재를 모르게 된다** |
| `/new-project` | ❌ | 되돌리기 어려운 것은 **사람이 시작한다** |
| `/review` | ❌ | 비용 · 🔴 `/codex:review` 자체가 사용자 전용이라 **막다른 길**이 된다 |

🔵 **미검증 ③에 절반의 답이 붙었다** — **커맨드는 뜬다.**
남은 건 `SKILL.md` 형태(예: `where-is-the-truth` · mattpocock 35개)가 **적절한 때에** 뜨는지다.

### 🆕 오늘 늘어난 검사 셋 — **전부 "적어놓고 안 따른 것"에서 나왔다**

| 검사 | 무엇을 막나 | 어떻게 발견됐나 |
|---|---|---|
| `check_audit_reachability` | `direction/` 이 **안 가리키는 감사 문서** — 다음 세션이 못 찾는다 | 구조를 세다가. 🔵 **내 첫 진단이 틀렸다** — 직접 참조만 세서 *"고아 13"* 이라 했는데 **전이 도달로는 3**이다 |
| `check_purpose_sync` | **현관문의 목적 한 줄이 정본과 갈리는 것** | 🔴 `README` 가 **두 판 뒤처져** rev7 이 *명시적으로 기각한 문구*를 들고 있었다 |
| `check_skill_firing` | 🔵 막지 않는다 — **계기다.** 스킬이 실사용에서 뜨는가 | 미검증 ③ 을 앞당기려다. **대안 둘을 재고 골랐다**(↓) |

**`check_skill_firing` 이 고른 이유** — `claude plugin eval` 은 🔴 **early access 게이트**(실행 전 거부) ·
OTel 텔레메트리는 🟡 **되지만**(수집기 없이 이벤트 12종 확인) **소급이 안 되고** `plugin.name` 이 **익명화**된다 ·
**세션 전사**는 🟢 **소급 가능 · 비용 0 · 켤 필요 없음.**

🔴 **그리고 시중 글 둘이 과장이었다** — `skill_activated` 이벤트도 `invocation_trigger` 속성도 **존재하지 않는다**(실행해서 확인).
그래서 ***"사용자가 쳤나 모델이 골랐나"* 는 어느 수단으로도 못 잰다** — 우리 한계가 아니라 **플랫폼 한계**이고
공식 요청 [`#35319`](https://github.com/anthropics/claude-code/issues/35319) 가 열려 있다.

⚠️ **지표도 고쳤다** — 첫 제안 `Skill ÷ 전체 도구호출` 은 **허영 지표**였다(스킬 하나가 `Bash` 50번을 이끌어도 1).
**사건으로 센다.** 첫 실측: **사건 13건 · 그중 우리 것 1건**(`kickoff`).

## 🔄 2026-08-28~29 — **완주 뒤 정비**가 끝났다 (PR 73건)

완주(2026-08-28)로 드러난 결함을 네 저장소에서 정비했다.
`standards` 42 · `workflows` 13 · `project-template` 8 · `divcal` 10.

**한 부류가 계속 나왔다 — *문서에만 있는 규칙은 발화하지 않는다*.** 그때마다 검사로 바꿨다:

| 새 검사 | 무엇을 막나 |
|---|---|
| `check_gaps_ledger` | 격차의 **종료 표기가 둘**이면 *"몇 건 남았나"* 에 답이 없다 (19 라 답했고 실제는 15였다) |
| `check_name_only_citations` | 코퍼스를 **이름으로만** 부르면 claim 불변식이 **우회된다** |
| `check_citation_status` | **덜 익은 근거가 익은 척**한다 (인용 14건 중 10건이 `draft`/`review-needed` 인데 병기 0) |
| `check_corpus_identity` | 코퍼스가 **폐기된 하네스 이름으로 자기를 소개**하고 있었다 |
| `check_dead_end_signals` | 요구 ⑥ — θ 를 **건강한 분포에서 재서**(브랜치 88개 · 최대 1) θ=2 |
| `check_decision_referrals` | 요구 ③ — 회부가 **사전등록된 곳에 0건**이었다 |
| `ci / pr-title` (`workflows`) | PR 제목 타입이 표준 11종인가 — **룰셋에도 넣었다** |

**바꾼 결정 셋** — 전부 *실측이 전제를 뒤집어서*다:
`project-template` 이 **태그를 단다**(copier 가 태그를 고른다) ·
확장 타입 어휘를 **접었다**(적어뒀는데 에이전트가 `record:`·`anchor:` 를 만들었다) ·
릴리스 노트는 **대체가 아니라 겹쳐 쓴다**(생성기는 색인이지 설명이 아니다).

## 📍 지금 정본은 어디인가 — **이 셋만 보면 된다**

| 묻는 것 | 정본 |
|---|---|
| **격차가 몇 건 남았나** | `python3 tools/check_gaps_ledger.py` — 지금 **6건** (⚠️ 이 줄은 **낡는다.** 🔴 **수를 인용하기 전에 명령을 돌려라** — 2026-09-01 에 이 줄이 5 로 남아 제3자에게 잡혔다) |
| **소유자에게 물어둔 것** | `gh issue list --label decision` — 지금 **0건 열림**(4/4 닫힘) |
| **이 저장소에서 일하는 법** | [`AGENTS.md`](AGENTS.md) — 44줄. `CLAUDE.md` 는 심볼릭 링크 |
| 🆕 **무엇으로 고르나** | [`direction/07`](direction/07-design-rules.md) — 원칙 넷과 **시행 기준**. 🔴 `04` 의 네 줄 표만 보면 **오독이 난다** |
| **배포는 어떻게 생겼나** | [`audit/PLUGIN-DESIGN`](audit/PLUGIN-DESIGN.ko.md) — 목적 다섯 → 자리. 후보는 **저장소로** 적는다 |
| **무엇이 깔려 있나** | `claude plugin list` — 지금 **5개** 전부 `enabled` |

## ⏭️ 다음 세션이 할 일

> 갱신 **2026-09-02 (`R5-45`·`R5-46` 을 둘 다 되돌린 뒤)**. 열린 격차 **6**(`check_gaps_ledger` 가 정본).
> 🔴 **둘 다 같은 이유로 되돌렸다** — 닫는 기준이 요구한 것 중 일부만 만들고 닫았다. **범위를 줄였으면 줄였다고 적고, 안 적었으면 안 닫는다.**
> 🔴 **지금 되는 것과 기다려야 하는 것을 갈라 적는다** — 섞으면 기다리는 것을 억지로 만들게 된다.

| # | 할 일 | 지금 되나 | 왜 그 순서 |
|---|---|---|---|
| **1** | **`R5-46` 남은 쪽 — 계약 시험** | 🟡 **수단이 안 보인다** | 벽 쪽은 섰다. *느슨해진 시험* 은 지금 **리뷰만** 본다 — 전부 막으면 TDD 가 죽고, 이름으로 고르면 사람이 매번 정해야 한다. **기대값이 약해졌는지를 재는 법**을 모른다 |
| **2** | **`R5-45` 남은 눈금 ⓓ — CI 대기시간** | ✅ 지금 | 🔴 **넷 중 둘만 섰다.** ⓐ 비율·ⓒ 회부 수는 있고 **ⓓ 는 워크플로 실행에서 바로 잰다.** ⓑ(kickoff→첫 초록)는 **제품 저장소가 하나뿐**이라 재료가 없다 |
| 3 | `R5-40` ⓒ 다음 층 — **리뷰 처분 계약** | 🟡 **20건 뒤** — 🔵 **다만 재료가 생겼다** | 표본 **15** / `ENOUGH=20`. 🔵 **처분 기록 없이 머지된 지적 18건 · PR 12개**(2026-09-01 신설 계기 `merged_open` · **오탐 0**) — **이 층이 기다리던 것**이다. 🔴 **인용 전에 `check_review_signal` 을 돌려라** |
| 4 | `R5-24` — 스킬 발화 | ⏳ **세션이 재료** | 할 일이 아니다. 굴리면 쌓인다 |
| 5 | `R5-26` — 에스컬레이션 3칸 | ⏳ **마찰이 재료** | 상상으로 만들면 **일곱 번째 하네스** |
| 6 | `R5-47` — 회부 표시의 **지속화 수단** | 🟡 **5건 초과 뒤** | `referrals_total` 지금 **5**. 지금 정하면 **표시 0~1건 상태에서 상상으로 고르는 것** |
| 7 | `R5-40` 닫기 | ⏳ `R5-24` 대기 | **ⓒ 는 2026-09-01 에 섰다.** ⓑ 만 남았다 |

### 🚫 지금 만들지 않는다 (제3자 감사와 판단이 일치했다)

Node 상자 · GitLab **추상화 층** · 스킬 확대 · 멀티에이전트 기본값 · 자체 런타임 ·
필수 검사 추가. **실제 수요가 나올 때 만든다.**
⚠️ **`R5-44` 의 GitLab 은 "만들기"가 아니라 "재기"였다** — 2026-09-01 에 **다 쟀고 닫혔다**.
**상자는 여전히 안 만든다**: 실제로 비공개를 원하는 프로젝트가 나올 때다.

### ⚠️ 굴리기 전에 알아둘 것

- **`third-party / review` 가 required 다.** 코덱스 **한도를 넘기면 머지가 막힌다** —
  기다리거나 룰셋에서 그 검사만 뺀다(관리자·1분)
- 문서를 고쳤으면 **`rebuild_after_manifest.py` → `build-routes.mjs`** 를 먼저 돌린다
- **`AGENTS.md` 의 `## Code Review Rules` 를 다른 변경과 같이 고치면 벽이 막는다** — 따로 낸다

## ⓪ 먼저 읽을 것 — 3개

| 순서 | 문서 | 왜 |
|---|---|---|
| 1 | [`direction/04-the-plan.md`](direction/04-the-plan.md) | **만들 것 13개 · 원칙 4 · 리서치에서 나온 수치 · 판정 기준** |
| 2 | [`direction/05-the-output-floor.md`](direction/05-the-output-floor.md) | **무엇이 저장소에 남아야 시니어급인가** (**12묶음**) · 아키타입 판정 |
| 3 | [`direction/02-why-past-attempts-failed.md`](direction/02-why-past-attempts-failed.md) §진단의 진단 | **같은 실수를 열 번 했다.** 열한 번째를 하지 않기 위해 |

나머지(`01`·`03`)는 **물어봤을 때만** 읽는다. `01`=요구 6가지의 근거, `03`=리서치 색인.

## 지금 상태

| | |
|---|---|
| 하네스 6세대 | **정리 끝** — 원격 3개 삭제(2026-08-24) · 로컬은 `~/Archive/` |
| 이 저장소 | 공개 · 룰셋 `main protection` 활성 · CI 초록 · **PR로만 머지 가능** |
| 벽 | **실물 확증 4/4** — 직접 푸시·빨간불 머지·`--admin` 강제 머지 전부 거부 |
| 만들 것 | **8/13** — ①②③④⑤⑦⑪⑫. ⚠️ **이건 구축 현황이지 목적 달성이 아니다** |
| 🔴 완료의 정의 — **범위** | **충족한 범위는 좁다** (정정 2026-09-01 · 제3자 감사가 지적): *공개 GitHub · Python · **신규** 프로젝트 · **단독 소유자** · **에이전트가 주행**.* 🔴 **목적 한 줄의 주어는 *비엔지니어*** 인데 `divcal` 완주는 **에이전트 주행**이었다 — **주어가 다르다.** **미검증**: 외부 설치자 · 기존 저장소 · 비공개 저장소 · Node 등 타 생태계 · **30일 유지** · 시니어 실제 인수 |
| 완주 | ✅ **2회** — #1→#2(따뜻한 세션) · **#4→#5(차가운 세션 자율)**. 9·10 회고까지 받았다 |
| 감사 (2026-08-29) | `repo_audit` **4저장소 `CLEAN findings=0 unknown=0`** · 드리프트 `CLEAN`(잉여 파일까지 본다) |
| 🆕 배포 (2026-08-29) | 플러그인 **둘** · 아스날 **넷** · 상시 **~381토큰**. `workflows` 가 마켓플레이스다 |
| 🆕 자기 검사 (2026-08-29) | `tools/` 에 **린트 12규칙군 + `mypy strict`**(R5-39 종료). 설정 정본은 `ruff.toml`·`mypy.ini` |
| 🆕 검사 (2026-08-30) | **11종** — 아래 §오늘 늘어난 검사 셋 |
| 정리 | ✅ **완료 2026-08-24 (2차)** — 원격 3개 삭제 · 로컬 5개 `~/Archive/`(1.1GB) · 홈 8표면 0건. ⚠️ 1차의 *"0건"* 은 **틀렸었다** — [2차 기록](legacy/judgments/harness-removal-record-2026-08-24.md) |

## 🎯 완료의 정의 — 이걸 먼저 읽어라

**이 저장소 안에서 도는 검증은 합성 시험이다.**
`standards`는 문서 저장소다 — `package.json`도 락파일도 테스트 스위트도 없다.
그래서 만들 것 대부분이 **여기서는 시험되지 않는다**:

| 여기서 시험되나 | 만들 것 |
|---|---|
| 🟢 된다 | ② 재사용 워크플로의 **기계장치**(호출이 도는가 · 검사 이름이 무엇이 되는가) · ⑫ gitleaks |
| 🔴 **안 된다** | ⑤ `new-project.sh`(새 저장소를 실제로 만들어야) · ③ 템플릿(인스턴스를 떠야) · ④ `/kickoff`(진짜 기획거리가 있어야) · ⑦ 세션 훅(이슈·마일스톤이 있어야) · ⑩ `floor-check`(락파일이 없어 FAIL만 난다) · **②의 내용**(`python-ci.yml` 은 uv 프로젝트가 있어야 돈다) |

> ⚠️ **위 🟢 칸의 첫 판은 *"이 저장소 `ci.yml`을 `uses:` 호출로 바꿔보기"* 였다. 그렇게 하면 안 된다.**
> 재사용 호출은 검사 이름을 `{호출잡}/{피호출잡}` 으로 바꾼다(실측 · 코퍼스 CPR-007).
> 이 저장소 룰셋은 `integrity` 를 요구하므로, 바꾸는 순간 그 이름이 영원히 보고되지 않아
> **자기 벽을 자기가 부순다.** 기계장치는 **별도 프로브 워크플로를 얹어서** 재는 것이 안전하다
> (기존 `integrity` 잡을 건드리지 않으므로 벽이 유지된다). 실제로 그렇게 쟀다 — PR #33.

### 그래서 완료의 정의는 이렇다

> **②③⑤가 끝나면 새 프로젝트를 하나 만들어 end-to-end로 돌린다.**
> `new-project.sh` → `/kickoff` → 이슈 → 브랜치 → PR → CI → 머지가
> **한 번 통과할 때까지 ①~⑤는 미완이다.**

**시험 프로젝트는 작고 진짜인 것**이어야 한다. 가짜 프로젝트로는 *아이디어→과제 번역* 구간이
시험되지 않는다 — 확증시험 세션 2가 소유자 결정으로 *"실제 만들고 싶은 아이디어"*를 쓴 이유다.

### 왜 이 규칙이 필요한가

goppi가 정확히 이 함정에서 죽었다:

> **합성 공격 4종은 0/4로 전부 막았는데, 실사용에서 프로덕션을 바꾼 명령 11건에는 발화 0건이었다.**
> **자기가 만든 시험만 통과한 것이다.**

도구를 만든 곳에서 그 도구를 시험하는 것은 같은 형태다.
리서치의 처방도 같다 — **walking skeleton: 첫 조각은 end-to-end 한 줄기**(Cockburn)
([`direction/05`](direction/05-the-output-floor.md) 바닥의 *테스트* 묶음 · ⚠️ 2026-08-26 정정 — 이전 판은 `03` 을 가리켰으나 거기에 없다).

## 순서 — ⑤를 먼저 하면 막힌다

첫 판의 권장 순서(⑤ → ② → ③)는 **의존이 정반대**라 쓸 수 없다:

| | 무엇이 먼저 있어야 하나 |
|---|---|
| ⑤ `new-project.sh` | `gh repo create --template coolbress/project-template` → **③** |
| ③ 템플릿의 5줄 `ci.yml` | `uses: coolbress/workflows/...` → **②** |
| ⑤ `ruleset.json` | 요구하는 context 를 **②가 실제로 내보내야** 한다 |

마지막 줄이 특히 위험하다 — ②가 없는 채로 룰셋만 걸면 필수 검사가 영원히 보고되지 않아
**머지가 불가능한 저장소**가 나온다.

**순서: ② ✅ → ③ ✅ → ⑤ ✅ → 실전 프로젝트 end-to-end.** ①은 ⑤가 돌면 자동으로 채워졌다.

## ✅ ③⑤ 완료 (2026-08-24) + 루프 1회 통과

| | |
|---|---|
| [`coolbress/project-template`](https://github.com/coolbress/project-template) | 공개 · `is_template` · **도는 uv 프로젝트**(빈 껍데기가 아니다) |
| [`coolbress/workflows`](https://github.com/coolbress/workflows) `new-project.sh` | 저장소 생성 + **서버 바닥 설치** — 벽 · 시크릿 탐지 · Dependabot · SHA 강제 · 머지 설정. **전 단계 fail-closed** ⚠️ *줄 수로 적지 않는다* (감사 E-5) |

### 템플릿이 빈 껍데기면 안 되는 이유 — 설계 제약 하나

`uv sync --locked` 는 **락파일이 없으면 실패한다.** 그런데 벽 때문에 빨간 상태에서는
아무것도 머지할 수 없다. 즉 **템플릿이 도는 프로젝트가 아니면 인스턴스의 첫 PR 부터 막힌다.**
그래서 템플릿에 `pyproject.toml` · `uv.lock` · 패키지 · 테스트가 실제로 들어 있고,
푸시 전에 로컬에서 5개(lint·format·typecheck·test·build)를 통과시켰다.

### 🔬 end-to-end 1회 통과 — 실측

`new-project.sh loop-probe` 로 만든 임시 저장소에서:

```
new-project.sh → 이슈(AC-1·AC-2) → 브랜치 → PR → CI 4/4 초록 → 머지 → 이슈 자동 종료
```

⑤가 실제로 건 것: `public` · `secret_scanning: enabled` · `push_protection: enabled` ·
룰셋 `active` · `우회: never` · 요구 검사 `ci / lint`·`ci / typecheck`·`ci / test`·`ci / build`.

**벽 4/4** (새 저장소에서 다시): 직접 푸시 `GH013` 거부 · 브랜치 허용 ·
빨간불 머지 거부 · **소유자 `--admin` 강제 머지 거부**(`Required status check "ci / typecheck" is failing`).

### 4개로 쪼갠 설계가 값을 한 지점

일부러 타입 힌트를 뺀 PR 에서:

```
ci / lint        pass   ← ruff 는 통과시켰다
ci / typecheck   fail   ← mypy strict 만 잡았다
ci / test        pass
ci / build       pass
```

한 잡 4스텝이었으면 *"CI 실패"* 하나만 보이고 **무엇이 왜 실패했는지 알 수 없었다.**

`loop-probe` 는 확인 후 **삭제했다** — 임시 프로브였다.

## ✅ 첫 완주 완료 — `divcal` (2026-08-27 착수 · **2026-08-28 완료**)

### 저장소를 만드는 데까지 나온 결함 넷 (2026-08-27)

**저장소를 하나 만드는 데까지 실제 결함 넷이 나왔다.** 전부 *"돌려보니 달랐다"* 에서 나왔다.

| | 무엇이 틀렸나 | 어떻게 드러났나 |
|---|---|---|
| **1** | `new-project.sh` 가 **재사용 워크플로를 allowlist 에 안 걸었다** | 감사 범위를 넓히자 즉시 — 그대로 뒀으면 **첫 CI 가 `startup_failure`** 로 죽고 저장소가 잠겼다 |
| **2** | 🔴 **붙여넣은 토큰의 공백 한 칸** | `gh api` 는 되는데 `git push` 만 거부. **헤더는 서버가 공백을 잘라내고 HTTP Basic 은 base64 안에 남긴다** |
| **3** | **실패를 너무 늦게 안다** | `uv lock`·`sync` 까지 다 하고 푸시에서 넘어졌다 → **클론 직후 푸시 선확인**으로 |
| **4** | 새 프로젝트를 **토큰 목록에 추가해야 하는데 아무도 안 알려준다** | 저장소는 완벽한데 **에이전트가 라벨 하나 못 만들었다**(403). 이제 스크립트가 말한다 |

🟢 **fail-closed 가 실사용에서 두 번 발화했고 두 번 다 정확했다** — 저장소를 만들고, 실패하고, **지웠다.**
**벽 없는 저장소를 안 남겼고 재시도 비용이 0 이었다.**

### 완료 조건 진행

| # | | 결과 |
|---|---|---|
| 1 | 저장소 생성 | ✅ (3번째 시도 — 위 결함 2·3 때문) |
| 2 | **서버 설정 확인** | ✅ squash 전용·자동삭제 · 시크릿탐지·푸시보호·Dependabot 전부 `enabled` · Actions `selected`+SHA강제+**패턴 2개** · CodeQL `configured` · 룰셋 1개·우회자 0·strict |
| 3 | `/kickoff` 인터뷰 | ✅ 4문항 · `[확인 필요]` 0개 |
| 4 | 여덟 답을 이슈 본문에 | ✅ [#1](https://github.com/coolbress/divcal/issues/1) — Issue Form 3칸 + 추가 5절 |
| 5 | 작은 실제 기능 + 테스트 | ✅ src 205줄 · 테스트 15개 |
| 6 | 브랜치 → PR | ✅ [#2](https://github.com/coolbress/divcal/pull/2) |
| 7 | required check 전부 보고·통과 | ✅ 6/6 |
| 8 | squash 머지 · `main` | ✅ 이슈 자동 종료 · 브랜치 자동 삭제 |
| 9 | 🔴 세션을 닫았다 다시 연다 | ✅ 2026-08-28 |
| 10 | 🔴 무엇을 못 찾는지 기록 | ✅ **아래 회고** |

⚠️ **`divcal` 을 `REPOS` 에 넣었다.** 기본 기대값(`PROJECT_CHECKS`·`PROJECT_ACTION_PATTERNS`)만으로
**그대로 통과**했다 — 오늘 *"새 프로젝트는 한 줄이면 끝나게"* 만든 설계가 실물에서 맞았다.

## 🎯 첫 완주 — 완료 조건 열 가지 (2026-08-27)

**저장소가 만들어지는 것은 완주가 아니다.** 여기까지 이어져야 한다:

| # | 무엇 |
|---|---|
| 1 | `divcal` 저장소 생성 (**저장소 밖에서** · 래퍼로 토큰 입력) |
| 2 | 생성 직후 **서버 설정 확인** — 아래 별도 표 |
| 3 | `/kickoff` 인터뷰 완료 |
| 4 | **여덟 답을 이슈 본문에 전부** 기록 (Issue Form 은 셋만 받는다) |
| 5 | **아주 작은 실제 기능 하나**를 테스트와 함께 |
| 6 | 브랜치 → PR |
| 7 | **required check 가 전부 실제로 보고되고 통과** |
| 8 | squash 머지 · `main` 확인 |
| 9 | 🔴 **세션을 닫았다 다시 연다** |
| 10 | 🔴 *"이 프로젝트 계속 진행해줘"* 만 말하고 **무엇을 못 찾는지 기록** |

### 🔬 9·10 결과 — **상태를 못 찾은 게 아니었다**

차가운 세션은 *"이 프로젝트 계속 진행해줘"* 한 줄만 받고 **7단계 만에 정확히 파악했다**:

```
README → git log → find → gh issue list → 소스 4파일 → gh issue view 1 → 로컬 검사
```

그 세션이 **혼자 이슈 [#4](https://github.com/coolbress/divcal/issues/4) 를 내고 PR [#5](https://github.com/coolbress/divcal/pull/5) 로 머지했다.** 회고가 남긴 것:

| | 못 찾은 것 | 회고 원문 |
|---|---|---|
| **①** | **다음 할 일의 순서** | *"백로그가 **부정형 목록**으로만 존재했다. 순위도 이유도 없어서 결국 임의로 추려 물어야 했다. **그 한 번의 질문이 저장소가 답해주지 못한 유일한 것**"* |
| **②** | `pyproject.toml` | 권한 거부 후 재시도 안 함 → *"줄길이 100 을 E501 에러에서 역산했다 — **설정을 읽어서가 아니라 깨져서**"* |
| **③** | `CONTRIBUTING.md` | README 와 PR 템플릿이 **두 번** 가리켰는데 **두 번 다 안 읽힘** |

🔴 **가장 중요한 관측 — 산문 속 포인터는 안 따라간다.** `CLAUDE.md` 가 `coolbress/workflows` 를
**명시**했고 *바닥·정본·생성기* 라는 단어를 다 봤는데 **하나도 안 열었다.** `~/standards` 는
존재조차 몰랐다. → **`NEXT.md` 를 아무리 잘 써도 `divcal` 세션에 안 닿는다**(훅 없음 · 링크 없음).
[`06`](direction/06-how-we-work.md) 의 **상태 probe 를 상상으로 설계하지 않아도 되게 됐다.**

🔴 **③ 이 제일 아프다.** `CONTRIBUTING.md`(57줄, 스텁 아님)가 이미 *"200줄 목표·400줄 상한"* 과
*"세는 건 사람이 읽을 diff 다 — 락파일·생성물은 따로 센다"* 를 적고 있었다. **그날 만든
`ci / diff-size` 의 설계가 거기 이미 있었는데 아무도 안 읽었다.**

### 그래서 고친 것 (2026-08-28)

| | 무엇 | 어디 |
|---|---|---|
| ① | 백로그 **9개를 열린 이슈로** + [#15 추적](https://github.com/coolbress/divcal/issues/15) 이 순서와 근거를 갖는다 | `divcal` |
| ②③ | `CLAUDE.md` → **`AGENTS.md`**(36줄) + 심볼릭 링크. 못 찾은 셋만 적었다 | `divcal` [#16](https://github.com/coolbress/divcal/pull/16) |

**AGENTS.md 에 상태 절은 안 넣었다.** 가변 상태는 `gh issue list` 가 갖는다 — 회고 세션이
시작 30초에 이미 그걸 돌렸다. ETH Zurich(138 repos·5,694 PR): **150줄 초과는 효용 체감**이고
*"비관련 내용은 선택적 무시가 아니라 **지시 전체의 무시**를 부른다."*

### 2번 — `divcal` 은 아직 자동 감사 밖이다

`repo_audit` 의 `REPOS` 에 **한 줄 더해야** 감사된다(템플릿 프로젝트는 기대값이 같아서 그거면 끝난다).
더하기 전까지는 손으로 확인한다:

Actions allowlist **두 패턴** · required check **전부** · CodeQL · secret scanning + push protection ·
Dependabot · 룰셋 **1개** · bypass **없음** · strict · squash 전용 · **실제 PR 의 모든 검사 보고**

## ✅ 그래서 완료의 정의는 **충족됐다** (2026-08-28)

```
new-project.sh → /kickoff → 이슈 → 브랜치 → PR → CI → 머지
                 ~~~~~~~~  ← 이 구간이 채워졌다
```

*아이디어 → 과제 번역* 구간이 **진짜 프로젝트에서 두 번** 돌았다. 두 번째는 차가운 세션이
**자율로** 했고, 그 세션이 낸 이슈 #4 에는 `/kickoff` 0절이 요구한 **시중 확인 절**이 들어 있다 —
Snowball Analytics · Sharesight(서비스)와 [`jhauberg/dledger`](https://github.com/jhauberg/dledger)(CLI)를 찾아 job 이 다른 이유 셋을 적었다.

### 오늘 들어간 것 (2026-08-28) — PR 9개 · 릴리스 2개 · 4저장소

| 저장소 | 무엇 |
|---|---|
| `workflows` | [#24](https://github.com/coolbress/workflows/pull/24) `ci / diff-size` 잡 · [#25](https://github.com/coolbress/workflows/pull/25) `/kickoff` 0절을 **시중 확인**으로 · [#26](https://github.com/coolbress/workflows/pull/26) 룰셋에 등재 · [#27](https://github.com/coolbress/workflows/pull/27) 멈춤 게이트 → **3갈래 판정** · **v3.2.0 · v3.3.0** |
| `project-template` | [#13](https://github.com/coolbress/project-template/pull/13) 핀 v3.2.0 |
| `divcal` | [#2](https://github.com/coolbress/divcal/pull/2) 첫 기능 · [#3](https://github.com/coolbress/divcal/pull/3) 핀 v3.2.0 · **[#5](https://github.com/coolbress/divcal/pull/5) 차가운 세션이 자율로** · [#16](https://github.com/coolbress/divcal/pull/16) `AGENTS.md` |
| `standards` | [#105](https://github.com/coolbress/standards/pull/105) 감사 기대값 · [#106](https://github.com/coolbress/standards/pull/106) 파이썬 버전 고정 · [#107](https://github.com/coolbress/standards/pull/107) 수치 축·표본 + 새 검사 |

🔴 **그날 배운 것 하나가 세 저장소를 관통한다 — *문서에만 있는 규칙은 발화하지 않는다*.**
`divcal` #1 이 이슈 본문에 *"400줄 이하"* 를 써놓고 436줄을 냈는데 **아무것도 막지 않았다.**
그래서 문장을 검사로 바꿨고(`ci / diff-size`), 같은 형태를 `standards` 에서도 찾아
(`X/Y` 표기를 축 없이 씀) **검사로 바꿨다**(`check_figure_citations`).

⚠️ **다만 계량기가 규칙만큼 중요했다.** 그 PR 을 새 계량기로 다시 재면 **385줄로 통과**한다 —
**규칙이 틀린 게 아니라 계량기가 틀렸었다**(README 산문이 코드와 같은 무게로 세어졌다).

### ✅ 그 결정은 이미 났다 (2026-08-28) — 이 절이 *"남은 것"* 으로 들고 있었다

**바닥의 `CONTRIBUTING` 요구는 `present` → `adequate` 로 이미 올라갔다.**
정본은 [`direction/05`](direction/05-the-output-floor.md) §*`CONTRIBUTING` 은 파일이 아니라 내용을 요구한다* 이고,
집행 수단까지 정해져 **돌고 있다** — 템플릿이 두 표지(`has_devflow` **AND** `has_prflow`)를 담은 파일을 주고
**인스턴스의 `ci / test`** 가 그걸 지킨다. 🔬 **실물 확인 2026-08-29**: `project-template` 의
`template/tests/test_contributing.py` · `divcal` 의 `tests/test_contributing.py` **양쪽에 있다.**
`census-gov-adequacy`(n=2,000·내용 파싱): present **61.5%** · adequate **41.2%** — **있는 것 중 1/3 이 스텁**이다.

🔴 **⑩ `floor-check` 의 첫 조각이 아니었다.** 새 만들 것을 짓는 대신 **템플릿에 이미 있던 패턴**을 한 번 더 썼다 —
`tests/test_env_example.py` 가 바로 이 통계를 근거로 인용해놓고 정작 CONTRIBUTING 검사는 안 쓰고 있었다.
**그러므로 ⑩ 은 이 결정에 걸려 있지 않다.**

🔬 **이 절이 하루 반 동안 틀려 있었다.** 결정은 8/28 에 정본(`direction/05`)에 들어갔는데
8/29 갱신에서도 이 요약은 *"소유자 결정 하나"* 를 들고 있었고, 최상단이 그걸 가리켜
**막고 있지 않은 것을 막고 있다고 읽히게** 했다. **요약이 원자료보다 오래 산다** —
`AGENTS.md` 🔴 ALWAYS 의 마지막 줄이 이것이고, **검사로 못 잡는다.**

⚠️ **`CONTRIBUTING` 자체는 바닥에서 빼지 않기로 확정했다** — 근거는 두 갈래다.
`census-governance-floor`(**n=6,582**)에서 별 구간을 고정해도 **`AGENTS.md` 채택 저장소가
CONTRIBUTING 을 더 갖는다**(`<1k` 63.3% 대 18.0% · `10k+` 83.9% 대 63.3%). 대체 신호가 없다.
시중 조사(30일)도 같은 방향이었다 — 대체 담론 자체가 없고, 오히려 **AGENTS.md 쪽이 역풍**을 맞는다.

## ✅ ④ 완료 (2026-08-24) — `/kickoff`

`coolbress/workflows` 의 `commands/kickoff.md`. `~/.claude/commands/` 에는 **사본이 아니라
심볼릭 링크**로 건다 — 본문이 벽 안에서 버전 관리되고 `git pull` 로 갱신이 전파된다.

코퍼스의 [`elicitation-interview-build-standard`](corpus/aspects/01-requirements-planning/elicitation-interview-build-standard.md)에서
도출했다: Mom Test 스타일 · 턴당 2문항 · 정보이득 질문 선택 · 위험 비례 깊이 8/12/18 ·
8개 커버리지 · `[확인 필요]` 마커 · AC 안정 ID + 검사 매핑 + `UNVERIFIABLE` · 잠금 게이트.

**핵심은 흔한 실수 목록이다.** 실증(RE'25 · arXiv 2507.02858)은 LLM 인터뷰어가
**그 목록을 쥐고 있을 때만** 사람보다 나은 후속 질문을 한다는 것을 보였다 —
있으면 좋은 게 아니라 이 커맨드가 값을 하는 이유다.

0번 절은 **"이미 있는 것인지 먼저 확인"** 이다. 이 확인을 건너뛴 실수가 **열 번** 있었고, **그 절이 이 세션에서 두 번 발화해 두 번 다 막았다**(`zipline Ledger` · `LedgerLens`).

## ✅ ② 완료 (2026-08-24) — `coolbress/workflows`

공개 저장소. main 보호됨. **직접 푸시 `GH013` 거부 실측.**

| 파일 | 무엇 |
|---|---|
| `.github/workflows/python-ci.yml` | 재사용 CI — **`lint`·`typecheck`·`test`·`build` 4개 별도 잡**. uv·ruff·mypy·pytest. Actions 전부 SHA 핀 · `permissions: contents: read` |
| `.github/workflows/ci.yml` | 자기 검사 — actionlint(다이제스트 핀). 잡 이름 `integrity` |
| `ruleset.json` | ⑤가 `--input` 으로 쓸 벽의 실물 |

> 🔵 **그 분리는 *플랫폼* 산물이기도 하다 (2026-08-30 · `GAPS` R5-44).**
> GitHub 룰셋은 **검사 *이름*** 을 요구하므로 개별 게이트를 만들려면 잡을 쪼개야 한다.
> **GitLab 은 *파이프라인 전체* 를 게이트한다**(`only_allow_merge_if_pipeline_succeeds` — 🔬 **2026-09-01 실측으로 확인**: 빨간 것도 **파이프라인이 없는 것도** 머지 405) —
> 거기서 잡 분리는 **진단 편의이지 벽의 요건이 아니다.** 🔴 **바닥이 요구하는 것은
> *넷이 각각 막는다* 이지 *잡이 넷이다* 가 아니다** — 다른 플랫폼으로 갈 때 이 구별을 안 하면
> **GitHub 의 형태를 규범으로 착각하게 된다.**

**4개로 쪼갠 이유**: 바닥이 *"lint·typecheck·test·build를 **각각 별도 required check**로"* 를
MUST 로 요구한다([`05`](direction/05-the-output-floor.md) §CI/CD). 한 잡 4스텝이면 첫 실패에서
멈춰 나머지 상태를 알 수 없고 룰셋이 개별 검사를 요구할 수도 없다.

### 🔴 ③·⑤가 반드시 지켜야 할 결합

검사 이름은 **`{호출잡}/{피호출잡}`** 이다(실측 · CPR-007). 호출잡을 `ci` 로 두면:

```
ci / lint     ci / typecheck     ci / test     ci / build
```

`ruleset.json` 이 요구하는 것도 이 네 이름이다. **템플릿의 호출잡 이름을 바꾸면 룰셋이
요구하는 이름이 영원히 보고되지 않아 저장소가 조용히 머지 불가로 잠긴다.**

### ② 에 그 뒤 들어간 것 · 아직 없는 것

*"루프가 한 번 초록으로 돈 뒤에 붙인다"* 는 규칙(walking skeleton · *벽보다 도구를 먼저 늘리기 ✕*)을
지켰고, 루프가 돌자 순서대로 붙였다:

| | 무엇 | 언제 |
|---|---|---|
| ✅ | **`ci / secrets`**(gitleaks) — 푸시 보호가 통과시키는 **개인키 PEM** 을 잡는다 | v3.1.0 |
| ✅ | ⑫ **SAST** — 공개는 CodeQL default setup ([근거](audit/SAST-CODEQL-VS-SEMGREP.ko.md)) | 2026-08-27 |
| ✅ | **`ci / diff-size`** — 리뷰 가능성 게이트. 룰셋 등재까지 | v3.2.0 · v3.3.0 |
| ⬜ | pipeline-guard (테스트 동반 검사) | — |
| 🔄 | ⑩ `floor-check` | **재판정 2026-08-29 — 새 잡은 안 만든다.** 명세 5개 중 3개가 이미 덮여 있었다(`direction/04` §⑩ 재판정). 템플릿 시험 + 드리프트 검사로 갈랐다 |
| 🔴 | **비공개 → Semgrep OSS** — *결정됐고 미구현*. `--private` 는 시작 전에 거부한다 | 측정 장비는 `workflows` 의 `research/c2-sast` 브랜치에 **주차돼 있다** |

✅ **`python-ci.yml` 의 내용은 이제 실제 프로젝트에서 돈다** — `divcal` 이 PR 4개를 그걸로 태웠다.

### ⑤의 실물 (참고 — 이 저장소에 걸린 것)

```bash
gh api repos/coolbress/<repo>/rulesets -X POST --input ruleset.json
```

`ruleset.json`의 핵심: `bypass_actors: []` (비우면 소유자도 못 넘는다 — 실측 확인),
`required_status_checks: [{context: "integrity"}]`, `pull_request.required_approving_review_count: 0`
(솔로는 자기 PR을 승인할 수 없다 — 승인 도장을 흉내내지 않고 CI를 진짜 게이트로 쓴다).

⚠️ **룰셋은 GitHub Free에서 공개 저장소에만 걸린다.** 비공개면 Pro가 필요하다
(실측: `403 Upgrade to GitHub Pro or make this repository public`).

## 🔴 하지 말 것 — 이 프로젝트가 여섯 번 무너진 이유

1. **하네스를 짓지 않는다.** 런타임에 도는 것을 만들면 그게 6세대다.
   판별식: **런타임에 도는가?** 돌면 하네스, 안 돌면 템플릿.
2. **로직을 설정 층에 넣지 않는다.** `new-project.sh`가 20줄을 넘어가면 그게 신호다.
3. **에이전트에게 규율을 프롬프트로 심지 않는다.** 벽은 GitHub에 있다.
4. **새로 짓기 전에 [`MAP.md`](MAP.md)를 주제어로 훑는다** (README 절대규칙 6).
   *"없다"* 고 단정하기 전에 확인한다 — 이 실수를 **열 번** 했다.
5. **지적을 받으면 그 좌표가 아니라 그 종류를 전 저장소에 grep한다** (절대규칙 7).

## 이 저장소에서 작업할 때

```bash
python3 tools/validate_corpus.py        # 구조·프론트매터·매니페스트·URL 대장
node    tools/build-routes.mjs --check  # 라우팅 지도 최신 여부
python3 tools/repo_audit.py             # 서버 설정 drift — 읽기만 한다 (4저장소)
python3 tools/check_pr_title_conformance.py   # PR 제목 규약 준수율 — 전환 조건 ⓑ (네트워크)
python3 tools/check_template_drift.py          # 인스턴스가 템플릿을 따라오나 (네트워크)
```

문서를 고쳤으면 `tools/rebuild_after_manifest.py`와 `build-routes.mjs`를 **다시 돌려야** CI가 통과한다.
`main`은 보호돼 있으므로 **브랜치 → PR → CI 초록 → 머지**로만 들어간다.

### 🔒 자격증명은 둘로 갈려 있다 (A-1 · 2026-08-27)

| | 무엇 | 누가 |
|---|---|---|
| **`GH_TOKEN`**(기본) | fine-grained — **쓰기**: Contents · Issues · PRs · Workflows / 🔑 **읽기**: + Administration · Code scanning | **에이전트** |
| 관리자 토큰 | classic(`repo`·`workflow`·`delete_repo`·`security_events`) · **30일 만료** | **사람만** — 이 컴퓨터에 **저장하지 않는다** |

🔴 **관리자 열쇠는 방에 없다** (2026-08-27). `gh auth logout` 으로 열쇠고리에서 뺐다.
관리자 작업은 **그때그때 붙여넣는다**:

```bash
cd ~   # 🔴 저장소 밖에서 — 스크립트가 저장소 안이면 멈춘다
~/workflows/tools/with-admin-token.sh ~/workflows/new-project.sh <이름>
```

🔴 **토큰을 명령줄에 쓰지 마라.** `GH_TOKEN=... 명령` 은 **`~/.zsh_history` 에 그대로 남는다**
(`histignorespace` 는 앞에 공백이 있을 때만 듣는다). 래퍼가 **물어본다.**

**그 프로세스가 사는 몇 초 동안만 존재한다** — 열쇠고리에도 파일에도 안 남는다.

🔑 **읽기를 준 이유**: 안 주면 `repo_audit` 이 **아무것도 확인하지 못한다**(`unknown=12`).
**감사기가 눈을 뜨는 것과 벽이 무너지는 것은 다른 문장이다.** 쓰기는 그대로 전부 403 이다 —
룰셋 수정·삭제 · Actions 정책 · 저장소 설정 · 환경 생성 · CodeQL 켜기 · 시크릿.

`~/.zshenv` 가 `~/.config/gh-agent-token`(0600)을 읽어 `GH_TOKEN` 에 건다.
**`.zshrc` 가 아니라 `.zshenv` 인 이유**: `.zshrc` 는 **대화형 셸만** 읽어 도구 셸에 안 닿는다.

🔴 **`env -u GH_TOKEN` 은 이제 아무것도 안 준다** — 실측: `You are not logged into any GitHub hosts`.
규율이 아니라 **없는 것**이다. 이전 판의 *"막히면 토큰을 빼고 실행하라"* 안내는 삭제했다.

**403 이 나면 그것이 정상이다.** 관리자 작업(룰셋 · Actions 정책 · 시크릿 · Environments ·
CodeQL default setup)은 **사람이 한다.** 에이전트는 명령을 만들어 넘긴다.

### ✅ `ci / deps` 가 벽이 됐다 (2026-08-30 · 소유자가 쳤다)

핀은 **다 올라갔다**([`workflows` v3.7.0](https://github.com/coolbress/workflows/releases/tag/v3.7.0) ·
[`project-template` v2.5.0](https://github.com/coolbress/project-template/releases/tag/v2.5.0) ·
`divcal` [#27](https://github.com/coolbress/divcal/pull/27)). 세 저장소 다 **`deps` 를 실제로 보고한다.**
룰셋 등재까지 끝났다 — **세 저장소 다 요구 검사에 `deps` 가 있고 우회자 0 · `active`.**
`repo_audit --repo` 로 넷 다 `CLEAN` 을 확인했고, `new-project.sh` 의 `ruleset.json` 과
감사기의 `PROJECT_CHECKS` 가 **집합으로 같다**(새 저장소는 태어날 때부터 받는다).

소유자가 친 것(관리자 토큰 · 기록용):

```bash
cd ~   # 🔴 저장소 밖에서
~/workflows/tools/with-admin-token.sh ~/workflows/tools/upgrade-ruleset.sh coolbress/workflows 'canary / deps:15368'
~/workflows/tools/with-admin-token.sh bash -c '
for r in project-template divcal; do
  ~/workflows/tools/upgrade-ruleset.sh "coolbress/$r" "ci / deps:15368"
done'
```

⚠️ **첫 줄만 이름이 다르다**(`canary`) — 아래가 이유다.
🔴 **여러 저장소는 한 줄로 묶는다** — 세 줄을 붙여넣으면 토큰을 세 번 쳐야 하고,
그 사이에 **줄이 삼켜진다**(아래 §토큰 프롬프트).

⚪ **`standards` 에는 안 걸었다 — 기각이지 누락이 아니다.** 실측: 의존성 그래프가
**넷뿐이고 전부 SHA 로 핀된 Actions** 다(파이썬 매니페스트가 없다 — `ruff`·`mypy` 는 CI 안에서
버전을 박아 `pip install` 한다). SHA 핀 + 허용목록이 이미 덮는 면을 위해 **관리자 작업 둘**
(허용목록 등재 · 룰셋 갱신)을 쓸 값이 아니다. 근거는 `repo_audit.py` 의 `EXPECTED_CHECKS` 밑에 있다.

#### 🔬 오늘 또 *적어놓고 안 따랐다* — 이번엔 검사로 잠갔다

이 문서 §*"③·⑤가 반드시 지켜야 할 결합"* 이 이미 말하고 있었다 —
**검사 이름은 `{호출잡}/{피호출잡}`.** `workflows` 는 자기 CI 를
`uses: ./.github/workflows/python-ci.yml` 을 **`canary`** 라는 잡으로 부르므로 이름이
`canary / deps` 다. 그런데 나는 세 저장소에 **같은 `ci / deps`** 를 쓸 명령을 만들었다.
**그대로 쳤으면 `workflows` 가 머지 불가로 잠겼다** — 되돌리려면 관리자 토큰이 또 필요하다.

읽어서 잡은 게 아니라 **이름을 실제로 조회해서** 잡았다. 그래서 그 조회를 스크립트에 넣었다:
[`upgrade-ruleset.sh`](https://github.com/coolbress/workflows/blob/main/tools/upgrade-ruleset.sh)
는 이제 **요구하려는 이름이 보고된 적 있는지 확인하고, 없으면 멈추고 실제로 온 이름을 보여준다**
(최근 커밋 3 + 최근 PR 5 의 check-run·커밋 상태 — `CodeQL` 은 기본 브랜치엔 안 뜨고 **PR 에만** 뜬다).
막기만 하면 사람이 고칠 수 없으므로 **맞는 이름을 같이 찍는 것**까지 시험한다
(`tests/upgrade-ruleset-guard.sh` 6건 · [#46](https://github.com/coolbress/workflows/pull/46)).

**같은 병의 세 번째다**(§2b 미준수 · 검사 끝 네 줄만 보기 · 이번). 앞의 둘은 기록으로 끝났고
**이번엔 검사가 됐다** — `direction/07` §01 *집행은 에이전트 밖에서*.

⚠️ 그 함정은 **두 곳에 적혀 있었다** — 이 문서 §결합과 `repo_audit.py` 의 `EXPECTED_CHECKS`
주석(*"`ci / diff-size` 를 걸면 … 자기잠금이 된다"*). **두 번 적어도 안 따랐다.**
읽어서 잡은 게 아니라 **이름을 조회해서** 잡았다 — 그래서 조회를 스크립트에 넣었다.

#### 🔬 토큰 프롬프트가 **다음 명령줄을 삼켰다**

세 줄을 한 번에 붙여넣었더니 첫 줄만 돌고 멈췄다. 래퍼가 토큰을 **stdin** 에서 읽어서,
셸이 들고 있던 **다음 명령줄이 토큰이 된다.** 고치기 전 판에 시험을 돌린 실측:

```
토큰 확인: 접두 LINE_ · 길이 15
⚠️ 접두가 낯설다 — …
```

**경고만 뜨고 멈추지 않는다.** 즉 그 명령은 **조용히 사라지고**(실행 안 됨) 앞 명령은
쓰레기 토큰으로 401 이 난다. 관리자 명령은 저장소를 만들고 벽을 고치는 것들이다.

[`with-admin-token.sh`](https://github.com/coolbress/workflows/blob/main/tools/with-admin-token.sh)
는 이제 **`/dev/tty` 에서 읽는다**(없으면 stdin 으로 되돌아간다 — CI 를 막지 않는다).
시험은 **pty 를 실제로 띄운다** — controlling terminal 을 주고 **stdin 만 파이프로 바꿔서**
남긴 줄이 살아 있는지 본다. `/dev/tty` 없이는 이 성질 자체를 확인할 수 없다.
🔴 **고치기 전 판에서 2건 FAIL 하는 것을 확인했다**
([#47](https://github.com/coolbress/workflows/pull/47) · `tests/token-prompt-not-from-stdin.sh`).

그 시험도 CI 가 한 번 잡았다 — `/bin/sh script` 로 실행했는데 우분투에선 `/bin/sh` 가 **dash** 라
`pipefail` 이 없다. macOS 는 `/bin/sh` 가 bash 라 **로컬에선 안 보였다.**

#### 그리고 템플릿 저장소엔 CI 파일이 **둘**이었다

처음 낸 [#23](https://github.com/coolbress/project-template/pull/23) 은
`template/.github/workflows/ci.yml`(**인스턴스에 실리는 것**)만 올렸다. 저장소 **자신의**
`.github/workflows/ci.yml` 은 그대로라 **이 저장소에선 `ci / deps` 가 안 돌았다.**
전부 초록이었다 — **검사 *목록*을 눈으로 세어서** 알았다. 초록은 *온 검사가 통과했다*는 뜻이지
*와야 할 검사가 다 왔다*는 뜻이 아니다.

## 열린 공백 — [`audit/GAPS.ko.md`](audit/GAPS.ko.md) §R5

🔴 **여기에 목록을 복사하지 않는다.** 이 자리에 있던 표가 `R5-2`·`R5-3`·`R5-6`·`R5-8` 을
**닫힌 뒤에도 열린 것으로** 들고 있었다(2026-08-29 발견 · 넷 다 8/24~8/29 에 종료). 대장은 한 곳이고 **세는 것은 검사다**:

```bash
python3 tools/check_gaps_ledger.py   # 열린 격차의 수와 번호 — 이 출력이 정본이다
```

**수와 번호는 위 명령이 낸다** — 여기 안 적는다. **왜 지금 못 하는지**는 §⏭️ 다음 세션이 할 일에 있다.

> 🔬 **2026-09-01 — 이 절이 자기 규칙을 어기고 있었다.** *"복사하지 않는다"* 고 적어둔 바로 다음 줄이
> 목록을 복사하고 있었고 **낡아 있었다**(`R5-27`·`R5-37` 은 닫혔고 `R5-45`·`R5-46` 이 빠졌다 —
> **수만 우연히 맞아서** 훑어봐서는 안 걸렸다). **규칙을 적는 것과 지키는 것은 다른 문장이다.**

## 2주 뒤 판정 기준

[`direction/04`](direction/04-the-plan.md) *"판정 기준 — 사전 등록"* 에 지표가 박혀 있다.
**결과를 보고 기준을 옮기지 않는다.**
