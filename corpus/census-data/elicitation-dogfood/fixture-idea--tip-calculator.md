# Idea — tip calculator (the builder's words)

> A tiny calculator: I type in a bill amount and a tip percentage, and it tells me the tip and the
> total. Sometimes I want to split the total across a few people. Command-line, just me, nothing saved.

This is the natural-language input. `prd.yml` in this directory is the **expected** elicited output.
The eval grader runs that `prd.yml` through the Slice-1 lock-gate (it must lock cleanly).
