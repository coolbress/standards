# frontier-ai-components / hooks-commands-subagents — raw evidence

Verbatim event names, frontmatter schemas, exit-code/JSON-I-O contracts, and real-project layouts harvested for
the orchestration/lifecycle layer — **Hooks · Slash-commands · Subagents** — backing
`aspects/27-ai-harness-archetype/hooks-commands-subagents-standard.md`. Append-only raw provenance — never edited.

Method: WebFetch of the Claude Code docs (`code.claude.com/docs/en/hooks`, `/sub-agents`, `/slash-commands` →
which now redirects to `/skills` because **custom commands merged into skills**), Anthropic's `multi-agent-research-system`
engineering post + the "how we use skills" blog, the OpenAI Codex docs (`developers.openai.com/codex/hooks`,
`/custom-prompts`, `/cli/slash-commands`, `/guides/agents-md`), the OpenAI Agents SDK docs (`openai.github.io/openai-agents-python/`
+ `/handoffs/`), and mature OSS (`ChrisWiles/claude-code-showcase`, `disler/claude-code-hooks-mastery`,
`VoltAgent/awesome-claude-code-subagents`, `hesreallyhim/awesome-claude-code`). Captured 2026-06-27.

See [`samples.md`](samples.md) for the harvested shapes (hook event table + exit-code semantics + JSON I/O,
subagent frontmatter, command/skill substitutions, the cross-vendor Anthropic↔OpenAI convergence table, the
multi-agent eval numbers, and the OSS-layout census).
